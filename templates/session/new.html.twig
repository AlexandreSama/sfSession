{% extends 'base.html.twig' %}
{% block title %} Créer une session {% endblock %} 
{% block body %}
<div class="mainNewFormation">
  <div class="holderMainNewFormation">
    <div class="titleHolderNewFormation">
      {% if edit %}
      <h1>Modifier une session</h1>
      {% else %}
      <h1>Créer une session</h1>
      {% endif %}
    </div>
    <div class="form">
        {{form(formCreate)}}
    </div>
  </div>
</div>
{% endblock %}