<nav class="navbar">
  <div class="holder">
    <div class="iconHolder">
      <img src="{{ asset('img/icon.png') }}" alt="" class="icon" />
      <h4>Kashir</h4>
    </div>
    <div class="linksHolder">
      <a href="{{ path('app_home') }}">Accueil</a>
      <a href="{{ path('new_formation') }}">Créer une formation</a>
      {# <a href="{{ path('new_session')}}">Créer une session</a> #}
      <a href="{{ path('new_category')}}">Créer une catégorie</a>
    </div>
    <div class="userStuff">
        <a href="{{path('app_logout')}}">Se déconnecter</a>
    </div>
  </div>
</nav>
