<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{path('app_home')}}" data-bcup-haslogintext="no">Kashir</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01"
      aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation" data-bcup-haslogintext="no">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarColor01">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <a class="nav-link active" href="{{path('app_home')}}" data-bcup-haslogintext="no">Accueil
            <span class="visually-hidden">(current)</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_formation')}}" data-bcup-haslogintext="no">Formations</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{path('app_session')}}" data-bcup-haslogintext="no">Sessions</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#" data-bcup-haslogintext="no">Cours</a>
        </li>
      </ul>
      {% if app.user %}
      <div class="dropdown-center navDropdown">
        <button class="btn btn-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
          <i class="fa-solid fa-user"></i>
        </button>
        <ul class="dropdown-menu">
          <li><a class="dropdown-item" href="#">Profile</a></li>
          <li><a class="dropdown-item" href="{{path('app_logout')}}">Se déconnecter</a></li>
          <li><hr class="dropdown-divider"></hr></li>
          <li><a href="{{path('new_category')}}" class="dropdown-item">Créer une catégorie</a></li>
        </ul>
      </div>
      {% else %}
      <div class="dropdown-center navDropdown">
        <button class="btn btn-primary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
          <i class="fa-solid fa-user"></i>
        </button>
        <ul class="dropdown-menu">
          <li><a class="dropdown-item" href="{{path('app_login')}}">Se connecter</a></li>
          <li><a class="dropdown-item" href="{{path('app_register')}}">S'inscrire</a></li>
        </ul>
      </div>
      {% endif %}
    </div>
  </div>
</nav>